# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling

# If using flakes, import this devenv configuration
# imports: []

# Allow unfree packages (if needed)
# allowUnfree: true
