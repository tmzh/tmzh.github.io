# Manual devenv activation - doesn't require source_url
# First ensure devenv is installed, then run: devenv shell

# Optional: Add devenv shell alias for quick access
export DEVENV_ROOT="$(pwd)"

# Check if devenv is available
if command -v devenv &> /dev/null; then
  eval "$(devenv print-dev-env)"
fi
